from setuptools import setup

setup(
  name             = 'apollo',
  version          = '0.1.0',
  description      = '',
  author           = '',
  author_email     = '',
  packages         = [
  ],
  install_requires = [
    'phe == 1.2.2'
  ]
)
